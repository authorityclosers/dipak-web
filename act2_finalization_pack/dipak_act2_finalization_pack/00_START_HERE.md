# Dipak Act 2 — Finalization Pass

## Scope lock

Do **not** start Act 3 or Act 4 yet.

This pack is only for making the existing Act 2 — **THE CERTAINTY BUILDER™** — feel finished enough for client review.

The current direction is worth preserving:
- editorial ivory canvas
- oversized serif identity statement
- founder line + verified bio
- three proof metrics
- Featured In rail
- restrained gold accents

The problem is not the concept. The problem is **composition precision, hierarchy, responsive fit, and transition subtlety**.

## Inputs reviewed

- Current desktop screenshot supplied by the user.
- New 14.67s recording supplied by the user.
- Agent walkthrough for local branch `feature/act2-certainty-builder`, local commit `e21b841`.
- Existing hero / Act 1 direction and the shared-motif scroll architecture.
- Previous Estate Autopilots motion research.

Important: the GitHub connector did not resolve `e21b841` / `feature/act2-certainty-builder` at the time of this review, so code-level line references are based on the file names and architecture supplied in the walkthrough, not a claim that the local branch was remotely fetched.

## Priority

1. Fix desktop title / rule collision.
2. Rebalance title and top-left index as one header composition.
3. Remove the washed-out crossfade during Act 1→2.
4. Normalize metric typography, especially `₹9+ CRORE`.
5. Fix the mobile metric clipping / horizontal overflow.
6. Rebuild Featured In as a real trust rail with monochrome brand marks.
7. Add one extremely subtle inherited brush fragment only if the static layout still feels too empty.
8. Re-run visual QA at transition states.
